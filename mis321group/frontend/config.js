/**
 * API base URL — must match the backend (see README).
 * Default matches the "http" launch profile: http://localhost:5253
 * If you use the "https" profile, set: "https://localhost:7131"
 */
window.APP_CONFIG = {
  apiBaseUrl: "http://localhost:5253"
};
